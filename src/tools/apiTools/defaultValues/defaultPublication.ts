import { ISinglePublication } from '../../../entities/publications';

const defaultPublication: ISinglePublication = {
	userId: 0,
	id: 0,
	title: '',
	body: '',
};

export default defaultPublication;
