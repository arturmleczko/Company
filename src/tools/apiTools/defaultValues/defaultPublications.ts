import { ISinglePublication } from '../../../entities/publications';

const defaultPublications: ISinglePublication[] = [
	{
		userId: 0,
		id: 0,
		title: '',
		body: '',
	},
];

export default defaultPublications;
